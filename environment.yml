name: base
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pandas
  - pyarrow
  - numpy
  - scikit-learn
  - seaborn
  - pytorch
  - pyserial
  - setuptools
  - pip
  - pip:
    - fastapi[standard]
    - pytest       
    - httpx        
    - pytest-cov      